import 'package:flutter/material.dart';

class AppTheme {
  // Primary color scheme
  static const Color primary = Color(0xFF121212); // Dark base
  static const Color secondary = Color(0xFF242424); // Slightly lighter dark

  //Button colors
  static const Color buttonPrimary = Color.fromRGBO(98, 0, 238, 1);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color.fromRGBO(173, 173, 173, 1);
}
